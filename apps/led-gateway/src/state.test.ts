import{describe,expect,it}from"vitest";import{fallbackState,LED_PRIORITY,type LedContent}from"./state.js";
describe("LED gateway state",()=>{it("keeps official priority order",()=>expect(LED_PRIORITY).toEqual(["EMERGENCY","ADMIN_OVERRIDE","ACHIEVEMENT","NORMAL_DASHBOARD","RUNNING_TEXT"]));it("uses unexpired cache when cloud is degraded",()=>{const content={id:"x",priority:"RUNNING_TEXT",title:null,body:"cached",version:1,ends_at:"2026-09-24T00:00:00Z"} satisfies LedContent;expect(fallbackState(content,new Date("2026-09-23T00:00:00Z").getTime()).mode).toBe("CACHED")});it("expires stale cached overrides",()=>expect(fallbackState({id:"x",priority:"ADMIN_OVERRIDE",title:null,body:"old",version:1,ends_at:"2026-09-22T00:00:00Z"},new Date("2026-09-23T00:00:00Z").getTime()).mode).toBe("FALLBACK"))});
