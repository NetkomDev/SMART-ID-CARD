import { Router } from "express";import { fromDatabaseError } from "../lib/errors.js";import { sendData } from "../lib/responses.js";import { asyncHandler } from "../middleware/async-handler.js";import { requirePermission } from "../middleware/tenant.js";import { validate } from "../middleware/validate.js";import { createWasteSchema } from "../schemas/waste.js";
const router=Router();
router.post("/transactions",requirePermission("waste.create"),validate({body:createWasteSchema}),asyncHandler(async(req,res)=>{const{data,error}=await req.auth!.client.from("waste_transactions").insert({...req.body,school_id:req.tenant!.schoolId,staff_user_id:req.auth!.user.id}).select("id,event_id,class_id,student_id,organic_kg,inorganic_kg,total_kg,source,created_at").single();if(error)throw fromDatabaseError(error);sendData(res,data,201)}));
router.get("/ranking",requirePermission("waste.read"),asyncHandler(async(req,res)=>{const{data,error}=await req.auth!.client.from("waste_transactions").select("class_id,total_kg,classes(name)").eq("school_id",req.tenant!.schoolId);if(error)throw fromDatabaseError(error);const totals=new Map<string,{class_id:string;class_name:string;total_kg:number}>();for(const row of data??[]){const c=Array.isArray(row.classes)?row.classes[0]:row.classes,old=totals.get(row.class_id)??{class_id:row.class_id,class_name:c?.name??"—",total_kg:0};old.total_kg+=Number(row.total_kg);totals.set(row.class_id,old)}sendData(res,[...totals.values()].sort((a,b)=>b.total_kg-a.total_kg))}));
router.get("/summary",requirePermission("waste.read"),asyncHandler(async(req,res)=>{const{data,error}=await req.auth!.client.from("waste_transactions").select("organic_kg,inorganic_kg,total_kg").eq("school_id",req.tenant!.schoolId);if(error)throw fromDatabaseError(error);sendData(res,(data??[]).reduce((a,r)=>({organic_kg:a.organic_kg+Number(r.organic_kg),inorganic_kg:a.inorganic_kg+Number(r.inorganic_kg),total_kg:a.total_kg+Number(r.total_kg)}),{organic_kg:0,inorganic_kg:0,total_kg:0}))}));
export{router as wasteRouter};
