import{z}from"zod";export const reportParamsSchema=z.object({domain:z.enum(["attendance","waste","library","extracurricular"])}).strict();export const reportQuerySchema=z.object({from:z.iso.datetime({offset:true}).optional(),to:z.iso.datetime({offset:true}).optional(),format:z.enum(["json","csv"]).default("json")}).strict().refine(v=>!v.from||!v.to||new Date(v.to)>=new Date(v.from),{path:["to"],message:"to must not precede from"});
