import{z}from"zod";export const createCardWriteJobSchema=z.object({card_id:z.uuid(),idempotency_key:z.uuid(),max_attempts:z.number().int().min(1).max(10).default(3)}).strict();export const claimCardWriteJobSchema=z.object({lease_seconds:z.number().int().min(30).max(600).default(120)}).strict();export const completeCardWriteJobSchema=z.object({job_id:z.uuid(),lease_token:z.uuid(),observed_rfid:z.string().trim().min(1).max(64),observed_qr:z.string().trim().min(1).max(128),retryable:z.boolean().default(false),error_code:z.string().trim().max(80).nullable().default(null)}).strict();
